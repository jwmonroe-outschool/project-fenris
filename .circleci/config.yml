version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: yarn build
  test:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: yarn test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
